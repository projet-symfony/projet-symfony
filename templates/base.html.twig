<!Dctml>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="stylesheet" type="text/css" href="{{ asset('CSS/header.css') }}">
        <link rel="stylesheet" type="text/css" href="{{ asset('CSS/left.css') }}">
        <link rel="stylesheet" type="text/css" href="{{ asset('CSS/footer.css') }}">
         {% block stylesheets %}{% endblock %}
    </head>

    <body>

    <header>
        <div id="banner">
            <img src="banner.jpg" alt="banniere">
        </div>

        <ul class="ulMenu">
            <li class="menu"> <a href="/" id="first"> ACCEUIL </a></li>
            <li class="menu"> <a href="dayprono">PRONOSTIC DU JOUR </a> </li>
            <li class="dropdown"> <a> CLASSEMENT </a>
                <div class="dropdown-content">
                    <a href="{{ path('classementpronostiqueur') }}">Pronostiqueur</a>
                    <a href="{{ path('classementclub') }}">Equipes</a>
                </div>
            </li>
            <li class="menu"> <a href="historique"> HISTORIQUE </a></li>
        </ul>

    </header>

    <div class = "leftLogin">

        <h4> {% block titleForm %} {%  endblock %} </h4>
        {% block form %} {% endblock %}

        {%  block register %}{% endblock %}

    <br /> <br />
        <h4> Top User </h4>

        {%  set count=0 %}
        {%    for lu in listTopUser %}
        {% set count = count + 1 %}
        <ul class="ulFirstUser">
            {% if count == 1 %}
            <li id="FirstUser">
                <span id="nbUser"> {{  count }} </span>
                <img alt="photoUser">
                <span id="nameUser"> {{ lu.login }}</span>
            </li>

            {%  else %}
            <li>
                <span id="nbUser"> {{ count }} </span>
                <img alt="photoUser">
                <span id="nameUser"> {{ lu.login  }}</span>
            </li>
            {% endif %}
        {% endfor %}

        </ul>
    </div>

    <div class="body">
        {% block body %}{% endblock %}

    </div>


    <footer>
        <p>NextTipster</p>
        <a>Qui sommes nous ?</a>
        <a>Nous contacter</a>
        <a>Plan du site</a>
        <a>Mentions légales</a>
        <a>FAQ</a>
    </footer>

    </body>

</html>

